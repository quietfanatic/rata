
#ifdef HEADER

struct Tester : Actor {
	<%= actor_info %>
	void serialize (Serializer* s) {
		dbg("Tester::serialize\n");
	}
	void act () {
		dbg("Tester::act\n");
	}
	void react () {
		dbg("Tester::react\n");
	}
	void draw () {
		dbg("Tester::draw\n");
	}
};

#else

#endif

