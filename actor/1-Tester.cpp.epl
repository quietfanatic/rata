
#ifdef HEADER

struct Tester : Actor {
	<%= actor_info %>
	void serialize (Serializer* s) {
		dbg("Tester::serialize\n");
	}
	void act () {
		dbg("Tester::act\n");
	}
	void react () {
		dbg("Tester::react\n");
	}
	void draw ();
};

#else

void Tester::draw () {
	img::rata_4::head->draw(Vec(1, 1), 0, 0, 1, 0);
}

#endif

